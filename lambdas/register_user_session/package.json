{
  "name": "register_user_session",
  "version": "0.1.0",
  "description": "AWS Lambda function that handles a user joining a game on their phone with the mobile pad UI.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "zip ../register_user_session.zip *"
  },
  "author": "Dan Hable",
  "dependencies": {
    "aws-sdk": "^2.650.0"
  }
}
